import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.yourname.lovecompatibility',
    appName: 'Love Compatibility',
    webDir: 'dist',
};

export default config;
